<li>
  <article>
    <header>
      <address><strong><%= comment.commenter %></strong> says...</address>
      <!-- <time><%= I18n.l comment.insert_dttm, format: :long %></time> -->
      <%= local_time(comment.insert_dttm) %>
    </header>
    <div class="comcont">
      <p><%= comment.body %></p>
    </div>
    <p> <%= link_to 'Remove Comment', [comment.article, comment],
                 method: :delete,
                 data: { confirm: 'Are you sure?' } %> </p>
  </article>
</li>
